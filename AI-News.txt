kimi "Build a full-stack AI news aggregator web application with the following specifications:FRONTEND:* Create a modern React or Next.js single-page application with a full-screen dark-themed UI (no wasted whitespace, use the entire viewport)* Display news in a responsive masonry or grid layout with headline, 200-character description, source badge, timestamp, and direct external link* Include a left sidebar filter panel showing all AI company sources with color-coded dots and article counts* Add a prominent 'Refresh All' button in the header with loading spinners* Implement auto-refresh every 15 minutes using JavaScript timers when the tab is visible* Use clean, readable typography with proper contrastBACKEND (Node.js/Express or Python/FastAPI):* Build a REST API that aggregates news from these sources every 15 minutes via cron job: o OpenAI Blog (RSS)o Google AI Blog (RSS)o Anthropic Blog (RSS)o Meta AI Blog (RSS)o Microsoft Research (RSS)o Hugging Face Blog (RSS)o NVIDIA AI Blog (RSS)o DeepMind Blog (RSS)o arXiv cs.AI (RSS)o MIT Technology Review AI section (RSS)o X/Twitter accounts: @OpenAI, @GoogleAI, @AnthropicAI, @DeepMind, @MetaAI, @MSFTResearch (use Puppeteer/Playwright or nitter/rss proxies to scrape without API keys where possible)* Store scraped articles in SQLite or MongoDB with deduplication logic (check by URL before inserting)* Normalize timestamps to UTC* Provide endpoints: GET /api/news (with ?source= filter), POST /api/refresh (trigger manual scrape), GET /api/sources (list active sources)SCRAPING LOGIC:* For RSS feeds: Use rss-parser or feedparser libraries* For X/Twitter without paid API: Use Playwright to scrape public profiles or use Nitter/RSS bridge instances as fallback* For other free media: Scrape TechCrunch AI, The Verge AI, Wired AI sections via RSS* Respect robots.txt and rate limits (add delays between requests)* Extract clean text descriptions by stripping HTML tags and truncating to 200 chars* Categorize each article by source company automaticallyDATA & BIAS MITIGATION:* Only extract from official company blogs, verified X accounts, and established tech journalism sources* Do not summarize or rewrite Ñ store original headlines and descriptions only* Tag each article with its original source URL so users always click through to the real source* Include a 'Sources' page listing all origins for transparencyDEPLOYMENT:* Create a Dockerfile for both frontend and backend* Provide docker-compose.yml for one-command local deployment* Add a README with setup instructions and environment variables* Ensure the backend can run on a schedule independently (cron inside container or external scheduler)PROJECT STRUCTURE:/news-aggregator/frontend (React/Next.js)/backend (Node.js or Python API + scraper)/database (migrations/schema)docker-compose.ymlREADME.mdStart by scaffolding the project structure, then build the backend scraper and API, then the frontend UI. Make sure the frontend consumes the backend API, not third-party services directly. Ensure all code is production-ready with error handling, logging, and proper async/await patterns."